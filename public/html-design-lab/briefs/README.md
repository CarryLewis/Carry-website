# Project design briefs

Write a brief **before** modifying another project.

The brief exists so the Lab does not impose its own aesthetics. Project context overrides generic patterns.

Copy [`_template.md`](_template.md) to `<project-slug>.md`.
