{
  "name": "laboratory-feedback",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "schema:remote": "wrangler d1 execute laboratory-feedback --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.32.0"
  }
}
